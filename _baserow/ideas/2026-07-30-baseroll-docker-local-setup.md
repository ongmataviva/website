# Idea: Baseroll local setup via Docker Compose

So the goal here is to use the docker compose to increase the minimum infrastructure of the base role here. We will probably need docker compose and Postgres running on the machine. And that's it. I want to run a minimum version of the docker compose, of the base role, the last version on my machine. And then, once it's running, we'll go after instructions on how to configure its IA.
